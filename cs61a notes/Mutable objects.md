Some objects in Python are mutable

lists 

dictionaries

other objects like numeric types and strings tuples are immutable

Lists:

method:

append(item)

extend(s)

insert(i, elem)

remove(elem)

pop(i)

pop()

